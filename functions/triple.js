"use strict";

/**
* Represents a triple function
* @param {Number} number The parameter
*/
function triple (number) {
  const newNumber = 3 * number;
  return newNumber;
}

module.exports.triple = triple;
